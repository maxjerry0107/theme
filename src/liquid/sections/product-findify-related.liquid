{%- if section.settings.enabled -%}
  <div class="findify-element" id="product-findify-rec-5"></div>
{%- endif -%}

{% schema %}
{
  "name": "Findify You May Also Like",
  "class": "section--product-findify",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Enable",
      "default": true,
      "info": "Enable or disable this section"
    }
  ],
  "presets": [{
    "name": "Findify You May Also Like"
  }]
}
{% endschema %}
