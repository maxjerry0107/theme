<!--Gorgias Chat Widget Start-->
<script id="gorgias-chat-widget-install">
  function loadGorgias() {
    var mobileMenuToggle = document.querySelector('#mobileMenuToggle');
    if (mobileMenuToggle){
      mobileMenuToggle.removeEventListener("touchstart", loadGorgias)
    }
    else {
      window.removeEventListener("load", loadGorgias)
    }
    !function (_) { _.GORGIAS_CHAT_APP_ID = "7923", _.GORGIAS_CHAT_BASE_URL = "us-east1-898b.production.gorgias.chat", _.GORGIAS_API_BASE_URL = "config.gorgias.chat"; var e = new XMLHttpRequest; e.open("GET", "https://config.gorgias.chat/applications/7923", !0), e.onload = function (t) { if (4 === e.readyState) if (200 === e.status) { var n = JSON.parse(e.responseText); if (!n.application || !n.bundleVersion) throw new Error("Missing fields in the response body - https://config.gorgias.chat/applications/7923"); if (_.GORGIAS_CHAT_APP = n.application, _.GORGIAS_CHAT_BUNDLE_VERSION = n.bundleVersion, n && n.texts && (_.GORGIAS_CHAT_TEXTS = n.texts), n && n.sspTexts && (_.GORGIAS_CHAT_SELF_SERVICE_PORTAL_TEXTS = n.sspTexts), !document.getElementById("gorgias-chat-container")) { var o = document.createElement("div"); o.id = "gorgias-chat-container", document.body.appendChild(o); var r = document.createElement("script"); r.setAttribute("defer", !0), r.src = "https://client-builds.production.gorgias.chat/{bundleVersion}/static/js/main.js".replace("{bundleVersion}", n.bundleVersion), document.body.appendChild(r) } } else console.error("Failed request GET - https://config.gorgias.chat/applications/7923") }, e.onerror = function (_) { console.error(_) }, e.send() }(window || {});
  }

  if(window.matchMedia("(max-width: 649px)").matches){
    window.addEventListener("load", function() {
      var mobileMenuToggle = document.querySelector('#mobileMenuToggle');
      mobileMenuToggle.addEventListener("touchstart", loadGorgias);
    }); 
  }
  else {
    window.addEventListener("load", loadGorgias);
  }
</script>
<script id="gorgias-chat-shopify-install">
  !function(_){_.SHOPIFY_PERMANENT_DOMAIN="{{shop.permanent_domain}}",_.SHOPIFY_CUSTOMER_ID="{{customer.id}}",_.SHOPIFY_CUSTOMER_EMAIL="{{customer.email}}"}(window||{});
</script>
<!--Gorgias Chat Widget End-->